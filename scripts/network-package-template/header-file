#ifndef _@UP_NAME@COMMAND_H
#define _@UP_NAME@COMMAND_H

#include <vector>

#include "NetworkPackage.h"
class @NAME@Command : public NetworkPackage {
    private:
@MEMBERS@

    public:
        virtual package_nr_t get_package_nr();
        virtual std::vector<unsigned char> encode_payload();
        virtual void decode_payload(std::vector<unsigned char> command_data);

@METHOD_DECLARATIONS@
};
#endif /* _@UP_NAME@COMMAND_H */
